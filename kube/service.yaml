---
apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  labels:
    app: helloworld
spec:
  type: NodePort
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 3000
    # nodePort: 32121
  selector:
    app: helloworld